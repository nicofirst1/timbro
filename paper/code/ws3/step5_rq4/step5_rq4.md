# WS3 step 5 — RQ4 temporal evolution (CONFIRMATORY H4a + H4b)

Generated by `paper/code/ws3/step5_rq4/step5_rq4.py` — do not hand-edit. Numbers are
the run's; the pre-registration is ADR-0005 §8b + `LEDGER_LOG.md#WS3-5-RQ4` (BINDING).

## Population (RQ4-eligible version chains)

- Chains (≥3 versions): **14382** · version-rows after the 18-row analyze-failure drop: **67146** (asserted against pre-registered 14388/67164 BEFORE the drop).
- Fork exclusion (ADR-0005): 0 multi-repo clusters → 0 fork chains dropped (a no-op — chains are single-repo canonical heads; forks removed upstream).
- Dropped 18 analyze-failed rows (null features, surrogate-char); kept 259 zero-token rows (log1p handles 0).

## Confirmatory results — MixedLM, version_index fixed effect, random intercept per skill_id

BH q=0.10 over the {H4a, H4b} family (D6-style). Per-version coefficient + 95% CI; one-sided direction pre-registered per hypothesis. Effect sizes reported, not bare p.

| hypothesis | per-version coef | 95% CI | p (raw) | p (BH, 2-family) | pre-reg side | supported |
|---|---|---|---|---|---|---|
| **H4a (length ↑)** | +0.02779 | [+0.02661, +0.02898] | 0.000e+00 | 0.000e+00 | coef>0 | **YES** |
| **H4b (converge ↓)** | -0.22808 | [-0.43699, -0.01918] | 3.236e-02 | 3.236e-02 | coef<0 | **YES** |

- **H4a** estimator: MixedLM(REML, random intercept per skill_id); N=67146 rows / 14382 chains; converged=True.
- **H4b** estimator: MixedLM(REML, random intercept per skill_id); N=67146 rows / 14382 chains; converged=True.
- H4b reference geometry: nearest of the **5** frozen RQ1 k-means centroids (clusters [0, 1, 2, 3, 4], RQ1-population sizes {0: 108698, 1: 4, 2: 30342, 3: 218, 4: 82994}), RQ1's frozen z-transform, no refit.

**H4b interpretive caveat (pre-registered, ADR-0005 §8b):** RQ1 (WS3-3-CLUSTER + robustness) found **NO discrete register dialects** — the k=5 partition is a below-silhouette-floor D3 fallback. So H4b measures convergence toward the nearest of the RQ1 k-means **reference centroids**, a partition RQ1 found dimensional not categorical — **NOT** convergence to a validated dialect. Any H4b reading carries this.

## Estimator diagnostics + OLS cluster-robust sensitivity

Both mixed models converged (no pre-registered non-convergence fallback triggered). The fitted between-skill random-intercept variance and any "singular RE covariance" optimizer warning are reported for honesty, alongside a warning-free OLS + CR1 cluster-robust-on-skill_id sensitivity estimate for the same `version_index` coefficient (NOT a swap — the confirmatory estimator remains the MixedLM; this is a cross-check).

| hypothesis | RE var (skill) | resid var | singular-RE warning | OLS-CR1 coef [95% CI] | OLS-CR1 p |
|---|---|---|---|---|---|
| H4a | 0.9041 | 0.3196 | no | +0.02793 [+0.00084, +0.05503] | 4.332e-02 |
| H4b | 9941 | 1.027e+04 | YES | -0.27637 [-0.44379, -0.10894] | 1.215e-03 |

- **H4b singular-RE note:** the warning fires because `zdist5` is on a large raw scale (resid var ≈ 1.027e+04), which ill-conditions the optimizer's internal covariance matrix — NOT a collapsed random effect (the fitted skill RE variance is 9941, comparable to residual, i.e. substantial between-skill variance). The OLS cluster-robust sensitivity above agrees on sign and significance, so the H4b conclusion does not hinge on the mixed-model conditioning. Still reported as a limitation.

## Descriptive within-chain slopes (one OLS slope per chain)

| outcome | median slope | IQR | fraction rising | fraction falling | n chains |
|---|---|---|---|---|---|
| log1p(desc_tokens) | +0.06146 | [+0.00000, +0.18145] | 69.1% | 16.6% | 14382 |
| zdist5 (to centroid) | -0.00631 | [-0.10662, +0.02188] | 33.2% | 53.1% | 14382 |

## Robustness — canonical-only rerun (fork-structure sensitivity)

The ~12360 canonical chains only (non-canonical single-repo chains removed). A sign flip vs the full population would flag that non-canonical chains drive the result.

| hypothesis | per-version coef | 95% CI | p (raw) | pre-reg side | matches full? |
|---|---|---|---|---|---|
| H4a | +0.02691 | [+0.02570, +0.02811] | 0.000e+00 | coef>0 | yes |
| H4b | -0.23600 | [-0.45816, -0.01384] | 3.734e-02 | coef<0 | yes |

## Calibrated summary

RQ4 (confirmatory, N=67146 version-rows / 14382 chains, MixedLM version_index fixed effect + skill random intercept, BH q=0.10 over 2): H4a (length rises): **SUPPORTED** (per-version coef +0.02779 [+0.02661, +0.02898], BH-p 0.000e+00); H4b (converges to register centroid): **SUPPORTED** (per-version coef -0.22808 [-0.43699, -0.01918], BH-p 3.236e-02). 69.1% of chains have a rising length slope; 53.1% move toward their nearest centroid. H4b carries its pre-registered caveat (RQ1 found the k=5 partition dimensional, not categorical — convergence is toward reference centroids, not a validated dialect). Within-skill (random intercept absorbs per-skill baseline); not causal. Canonical-only robustness reported alongside. The lagged-adoption + embedding-delta exploratory analyses (ADR-0005 §8b) are out of scope for this confirmatory run.
